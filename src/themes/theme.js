import { createTheme } from '@mui/system';

const theme = createTheme ({
  palette: {
    primary: {
      main: '#e0cbbe',
    },
    secondary: {
      main: '#b3dec1',
    },
  },
},
);

export default theme;